# HARDENING_BACKLOG — отложенные работы по надёжности и безопасности

## Статус

```text
Заведён 22.09.2026 после приёмки этапа 13 (Stage 13 = ACCEPTED / PASS).
Принятая контрольная точка production: CRM master fe88cfe2f8fd36d33211ac29135daa8d5b27c6f2
(backend и frontend — revision и /health.build тот же), сайт feature/cms-admin 441d795.
Этап 13 повторно не выполняется. Всё, что ниже, — работы ВНЕ текущего этапа: каждая идёт
отдельным заданием Reviewer, массово не выполняются.
```

## 1. Запреты, действующие до отдельной команды

| Запрет | Почему | Где закреплено |
|---|---|---|
| Не публиковать `:latest` | оба сервера потребляют `:production`; плавающий `latest` 14.09.2026 уже подменял бой сборкой из устаревшей ветки | `.github/workflows/build-images.yml`, тест `crm-new/src/deploy-safety.spec.ts`, H6 |
| Не включать retention `--apply` | удаление журналов необратимо; dry-run 22.09 показал 0 строк под правилами — спешить некуда | `RETENTION_POLICY.md`, `13_PRODUCTION_ROLLOUT.md` § 8 |
| Не выполнять H1–H8 массово | каждый долг меняет разный слой (миграции, nginx, docker, sshd, GitHub) и требует своего окна и проверки | `13_PRODUCTION_ROLLOUT.md` § 10 |
| Не сливать `docs/analytics/13_PRODUCTION_ROLLOUT.md` в `master` отдельно | пуш в master поднимает CI и новую сборку — production уйдёт с принятой точки fe88cfe ради документации | решение Reviewer 22.09.2026 |

Документ этапа 13 (`13_PRODUCTION_ROLLOUT.md`, коммиты 24a47fe, fe88cfe, f453a50 в `feature/analytics-foundation`)
включается в **следующий контролируемый merge** вместе с кодом очередного этапа.

## 2. H2 — access-логи без рекламных идентификаторов (следующая работа по hardening)

Задача заведена как первая к исполнению: она не трогает данные и обратима правкой конфигурации.

```text
ГДЕ       обе конфигурации nginx, а не одна:
          1) сайт   — web-photo/nginx-domain.conf (контейнер photo-web / фронт сайта)
          2) панель — frontend/nginx-photo.conf (контейнер raspechatka-frontend-1)
ЧТО       убрать query-string из строки access-лога либо маскировать yclid:
          log_format site '$remote_addr - [$time_local] "$request_method $uri" $status $body_bytes_sent "$http_user_agent"';
          либо map $args → $args_safe с заменой yclid=<цифры>
ПОЧЕМУ    22.09.2026 подтверждено: yclid пишут ОБА access-лога. В логе панели за час — 4 строки
          вида «GET /?yclid=<19–20 цифр>» от робота Яндекс.Метрики. Это рекламный идентификатор,
          не секрет и не PII в строгом смысле, но спецификация этапа 13 относит его к скану.
РЕШЕНИЕ ПО IP  остаётся за владельцем: $remote_addr — стандартная строка nginx, убирать её не обязательно.
```

### Обязательная проверка BEFORE/AFTER

```text
BEFORE   для каждого из двух контейнеров зафиксировать:
           docker logs --since 1h <контейнер> | grep -cE 'yclid=[0-9]{15,}'        → ожидаем > 0 хотя бы на одном
           пример строки (обезличенный, значения не выводить: yclid → <19-20-ЦИФР>, IP → <IP>)
           общее число строк access-лога за окно — чтобы AFTER сравнивался на сопоставимом трафике
ACTION   правка log_format в обеих конфигурациях → сборка образов через CI → выкладка штатным
         auto-update (без --no-deps ничего не делать вручную), nginx перечитывается автоматически
AFTER    те же две команды на том же окне (не меньше часа реального трафика, включая робота Метрики):
           совпадений yclid → 0 в обоих контейнерах
           строки access-лога продолжают писаться (счётчик > 0) — иначе сломан сам лог
           коды ответов и объёмы в логе совпадают по смыслу с BEFORE
STOP     access-лог опустел или перестал писаться; сайт/панель отдают не 200 на / и /interer/holst;
         сборка не прошла CI
ROLLBACK вернуть прежний log_format тем же путём (CI → auto-update); откат конфигурации данных не касается
```

## 3. Остальные долги (H1, H3–H8) — без изменений, по отдельному заданию

| # | Долг | Кратко |
|---|---|---|
| H1 | Drift `SalaryPayment.updatedAt` (DEFAULT при `@updatedAt`) | отдельная миграция `DROP DEFAULT` обычным деплоем; до этого drift считается известным отклонением |
| H3 | Docker json-file без ротации | `daemon.json` с `max-size`/`max-file` (рестарт docker = простой 1–2 мин) либо `logging:` в compose по сервисам |
| H4 | `sshd PasswordAuthentication yes` | `no` + `KbdInteractiveAuthentication no` только после подтверждения входа по ключу из второй сессии |
| H5 | GitHub branch protection / environment | защита `master` и `feature/cms-admin`, environment `production` для шага публикации меток |
| H6 | Удаление `:latest` | после ≥ 7 дней стабильной работы обоих серверов на `:production` |
| H7 | Скан секретов в CI | gitleaks или grep по шаблонам `SECRET_ROTATION_RUNBOOK.md` § 1 в job `test` обоих репозиториев |
| H8 | `lastDailyDate` планировщика в памяти | хранить в БД, чтобы рестарт не вызывал лишний суточный тик |

## 4. Факты эксплуатации, которые нужно учитывать в любой из этих работ

```text
1. Планировщик привязан к моменту старта контейнера (см. SCHEDULER_RELIABILITY.md § 5):
   после пересоздания два наблюдаемых автоцикла = boot-цикл + следующий часовой цикл,
   то есть окно наблюдения после выкладки — не меньше часа с небольшим.
2. Выкладку делает только auto-update по метке :production; ручной запуск и таймер
   защищены единым замком (flock /var/lock/auto-update.lock) — второй проход выходит с «Пропуск».
3. Сверка сборки ждёт готовности контейнера ограниченно (12 попыток × 5 с) и различает
   «ещё не поднялся» и «работает не та сборка» — при hardening-выкладках смотреть именно эти строки.
```

## 5. Качество данных — задачи после финальной приёмки (этап 14 ACCEPTED, 22.09.2026)

Архитектура аналитики закрыта: цепочка трафик → заявка → заказ → статусы → Метрика → деньги →
dashboard → вывод проверена на реальных данных, внутренние сверки дали 0 расхождений. Всё, что
осталось, — полнота и качество ВХОДНЫХ данных. Порядок — по пользе, как расставил Reviewer.

| # | Что | Почему это следующий шаг | Что считать результатом |
|---|---|---|---|
| D1 | `clientPaidAt` — фиксировать дату оплаты | суммы уже верны, но 40 из 91 оплаченных заказов 30-дневного окна без точной даты смещают временные когорты: выручка признаётся датой отправки | **ROLLED OUT / MONITORING (22.09.2026, production 6323bc6)** (аудит — `D1_CLIENT_PAID_AT_AUDIT.md`, реализация — `D1_CLIENT_PAID_AT_IMPLEMENTATION.md`): ручной PAID принимает фактическую дату, у оплаченных без даты есть ADMIN-действие «указать дату оплаты», выплата зарплаты дату не выдумывает. Миграции нет, backfill нет, при выкладке ни одна дата не изменилась. Закрывать рано: польза подтверждается на следующих реальных оплатах — наблюдаем за `paidWithoutDate` (30д 40, 7д 1 на момент выкладки) и за тем, что новые ручные PAID получают дату |
| D2 | Покрытие ClientID | 10,6 % (30 дней) и 19,4 % (7 дней) при пороге 50 % — рекламная атрибуция охватывает меньшинство заказов CRM | рост `dataQuality.clientIdCoverageAccepted`; строк очереди со `skipReason = no_client_id` становится меньше |
| D3 | H2 — `yclid` из access-логов | приватность; подробности и порядок BEFORE/AFTER — в § 2 этого файла | 0 совпадений `yclid` в логах обеих конфигураций nginx при живом трафике |
| D4 | UTM | система метки поддерживает и хранит, но текущий трафик ими почти не пользуется: за окна приёмки 0 заказов с UTM и 588 визитов без меток | первая кампания с метками → проверить UTM-цепочку end-to-end (пункт 10.2 мастер-плана сейчас NOT OBSERVED, а не закрытый) |
| D5 | `firstTouchUrl` | поле заполняется с 13.09.2026; исторические заявки его не имеют | просто накопить историю; **восстанавливать задним числом не нужно** |

Ограничения прежние: ничего из этого не выполняется без отдельного задания, production не меняется,
`:latest` не публикуется, retention `--apply` не включается.

Два правила, закреплённые работой D1 и обязательные для всех будущих работ этого списка:

```text
SALARY PAYOUT IS NOT CUSTOMER PAYMENT DATE — расчёт с исполнителем не является датой оплаты клиентом.
HISTORICAL NULL clientPaidAt VALUES ARE NOT BACKFILLED OR INFERRED — неизвестные даты остаются неизвестными.
```
