# Транзиентное содержимое графа: кэш сборки, генерируемая визуализация и
# ежедневные бэкапы кураторского графа. Коммитим только сам граф (graph.json),
# отчёт и метаданные — остальное перестраивается из кода командой `graphify update .`.
cache/
graph.html
20[0-9][0-9]-[0-9][0-9]-[0-9][0-9]/
memory/
reflections/
