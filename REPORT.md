# CRM Audit Report — 2026-06-14

Аудит охватывает весь бэкенд (`crm-new/src/`) и фронтенд (`frontend/src/`).

---

## Исправленные проблемы

### CRITICAL

| # | Проблема | Файл | Статус |
|---|---------|------|--------|
| C-1 | Исполнитель получал ВСЕ заказы, а не только свои | `order-photo.service.ts:getAllOrders` | Исправлено |
| C-3 | Деактивированный пользователь мог авторизоваться и работать весь срок жизни JWT (7 дней) | `auth/jwt.strategy.ts:validate` | Исправлено |
| C-4 | Начисление могло зависнуть в статусе PARTIALLY_PAID при paidAmount >= salaryAmount | `salary.service.ts:getSummary` | Исправлено |

### HIGH

| # | Проблема | Файл | Статус |
|---|---------|------|--------|
| H-7 | deleteUser не проверял, назначен ли пользователь на активные заказы | `users.service.ts:deleteUser` | Исправлено |
| H-8 | Запись в историю ставок и обновление пользователя — два отдельных вызова без транзакции | `users.service.ts:updateUser` | Исправлено |

### MEDIUM

| # | Проблема | Файл | Статус |
|---|---------|------|--------|
| M-3 | Параметр year в reports не валидировался — при year=abc сервер возвращал 500 | `reports.controller.ts` | Исправлено |
| M-10 | Деактивированные исполнители с нулевым долгом показывались в списке зарплаты | `SalaryPage.tsx` | Исправлено |
| M-11 | Удаление расходного ордера выполнялось без подтверждения | `ReportsPage.tsx` | Исправлено |

### LOW

| # | Проблема | Файл | Статус |
|---|---------|------|--------|
| L-1 | Мёртвая переменная datePrefix с void datePrefix | `order-photo.service.ts` | Удалено |
| L-5 | PrismaService не реализовывал OnModuleInit/OnModuleDestroy | `prisma/prisma.service.ts` | Исправлено |

---

## Открытые проблемы (требуют обсуждения или отдельного PR)

### HIGH

| # | Проблема | Рекомендация |
|---|---------|-------------|
| H-1 | updateOrder вычисляет totalOrder из данных до транзакции (race condition) | Обернуть в $transaction с SELECT FOR UPDATE |
| H-2 | tshirt-item.service.ts: мутация + recalcTotal без транзакции | Обернуть в $transaction |
| H-3 | order-item.service.ts: то же | Обернуть в $transaction |
| H-4 | assignExecutor не обрабатывает смену исполнителя при статусе PAID | Задокументировать или заблокировать |
| H-5 | Генерация номера заказа: LIKE '202506%' может зацепить 20250630 при старте июля | Использовать точный префикс или таблицу-счётчик |

### MEDIUM

| # | Проблема | Рекомендация |
|---|---------|-------------|
| C-2 | POST /order-photo/lead без rate limiting | Добавить @nestjs/throttler |
| M-4 | completedAt ставится при COMPLETED, а начисление создаётся при SENT — дата в зарплате всегда null для PHOTO | Добавить поле sentAt |
| M-5 | Фронтенд не знает о SLEEVE_LEFT, SLEEVE_RIGHT, FULL в EnumPrintLocation | Синхронизировать типы |
| M-6 | Поля gender и printType у ItemTshirt не отображаются в CRM | Добавить в тип и компонент |
| M-7 | createOrder принимает любой EnumStatus в DTO | Ограничить допустимые значения |
| M-8 | Легаси-эндпоинт GET /order-photo/salary/summary использует хардкод 30% | Удалить легаси-эндпоинт |
| M-9 | На 401 фронтенд делает window.location.href вместо navigate() | Использовать React Router navigate |
| M-12 | Счётчики статусов на странице заказов — только по текущей странице | Серверная агрегация |
| L-4 | Хардкод реквизитов в OrderDetail.tsx | Вынести в siteConfig.ts |
| L-10 | window.open для PDF после async — браузер блокирует как попап | Открывать синхронно или использовать Blob URL |

---

## Новые фичи задеплоены в этой сессии

1. Ссылка на общение в таблице начислений (иконка рядом с номером заказа)
2. Смена исполнителя на SENT: пересчёт начислений автоматически
3. Выплата зарплаты не меняет статус заказа (остаётся SENT)
4. Расходные ордера: категория (TSHIRT/PHOTO), сумма, примечание
5. Финансовый отчёт: Расходы (фото + одежда), ЗП выплачено, Прибыль
