-- Своё главное фото у каждого цвета: футболка на снимке должна быть того же
-- цвета, что и вариант. Пусто — берётся главное фото принта, поэтому у уже
-- заведённых карточек ничего не меняется.
ALTER TABLE "OzonVariant" ADD COLUMN "mainPhotoUrl" TEXT;
