-- Цена по умолчанию для новой карточки — 1500, а не 3500.
--
-- Это умолчание колонки для НОВЫХ кабинетов. У уже существующего шаблона
-- значение хранится в строке (у владельца сейчас 500), и меняется оно
-- в настройках шаблона, а не здесь: миграция колонку существующих строк
-- не трогает, чтобы не перебить осознанно выставленную цену.
ALTER TABLE "OzonCatalogTemplate" ALTER COLUMN "defaultPrice" SET DEFAULT 1500;
