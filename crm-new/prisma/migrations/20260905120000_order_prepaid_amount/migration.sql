-- Фактическая предоплата клиента по заказу.
--
-- Раньше предоплата и остаток всегда считались как 50% от суммы. Когда
-- клиент после предоплаты добавлял/убирал позиции, сумма менялась — и
-- «остаток» пересчитывался от нового 50%, хотя клиент уже внёс конкретную
-- сумму. Теперь фактическая предоплата хранится, и остаток = сумма − она.
-- NULL = предоплату ещё не вносили (показываем ориентир 50%).
ALTER TABLE "OrderPhoto" ADD COLUMN "prepaidAmount" INTEGER;
