-- Кого всегда тегать при заявке с сайта.
-- Менеджеры по оформлению в приоритете над админами, поэтому владелец,
-- не будучи менеджером, не попадал в тег вообще. Список задаётся отдельно.
ALTER TABLE "PartnerSettings"
  ADD COLUMN "leadMentionUsernames" TEXT NOT NULL DEFAULT '';
