/**
 * Готовые названия для позиции со свободной ценой.
 *
 * Главный случай — печать на вещи, которую принёс клиент: футболки в заказе
 * нет, есть только наша работа. Такую позицию нельзя завести обычной строкой
 * (там цена умножается на количество и требуется цвет с размером), поэтому её
 * заводят свободной ценой, а название набирали руками — с разнобоем в
 * формулировках от заказа к заказу. Кнопки убирают этот разнобой.
 */
export const FREE_PRESETS = [
  'Нанесение принта на изделие заказчика',
  'Печать на изделии заказчика',
] as const;

/** Пояснение под полем цены: количество на неё не влияет. */
export const FREE_PRICE_HINT =
  'Договорная сумма за всю позицию — на количество не умножается.';
