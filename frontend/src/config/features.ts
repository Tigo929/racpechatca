/**
 * Флаги модулей, которые можно включить/выключить одной строкой.
 *
 * Идея: фича на этапе теста живёт за флагом. Не зашла — ставим false, и она
 * исчезает целиком (пункт меню, маршрут), не задевая остальной CRM. Включаем
 * обратно — тоже одной строкой. Так же, как закомментированный пункт Avito в
 * navigation.ts, только без правки нескольких мест.
 */
export const features = {
  /**
   * «Дизайнер принта»: грузишь фото → они вписываются в буквы шаблона (маска
   * по вектору) → экспорт PNG (прозрачный фон, DTF). Для индивидуальных
   * принтов с маркетплейса. Пока на тесте.
   */
  printDesigner: true,
} as const;

export type FeatureKey = keyof typeof features;
